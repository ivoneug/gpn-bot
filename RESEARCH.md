# Ресерч API gpnbonus.ru (карта АЗС «Газпромнефть»)

Дата исследования: 2026-08-16

## Итог коротко

Наличие топлива на АЗС отдаёт **один незащищённый endpoint** — `POST https://gpnbonus.ru/api/stations/list`.
Ни авторизации, ни CSRF, ни cookie не требуется. Отдаёт **все 1925 АЗС по всей России** одним ответом
(3.9 МБ JSON, ~165 КБ по проводу с gzip, ~1–2.3 с). Фильтрация по регионам/городам на бэкенде **не поддерживается** —
фронт фильтрует всё локально, значит и боту придётся делать то же самое.

Данные живые: между двумя вызовами с интервалом ~1 минута изменились флаги наличия у 10 АЗС по стране.

## Как нашёл

Страница `/fuel/refuel-map` — Laravel + Vue. Компонент `<refuel-map :region="...">` живёт в бандле
`https://gpnbonus.ru/build/assets/app-DhDj1rt2.js`. В нём (компонент `RefuelMap`, метод `load()`):

```js
axios.post(`/api/stations/list`, this.filter).then(e => { ... })
```

Прочие найденные endpoint'ы: `/api/fuels/stations`, `/api/promotion/stations`, `/api/stations/rate`,
`/api/services/list`, `/api/news/list`, `/api/help-feedback/add`.

Важно: `/api/fuels/stations` (тело `{"fuels":[62,12],"region":2612668}`) возвращает лёгкий ответ 30 КБ,
но это **ассортимент/линейка топлива АЗС, а не текущее наличие** — вернул все 65 АЗС края, включая те,
где 92 и 95 сейчас `false`. Для мониторинга не годится.

## Основной запрос

```bash
curl -sS --compressed \
  -H 'Content-Type: application/json' \
  -X POST https://gpnbonus.ru/api/stations/list \
  -d '{}'
```

- Метод: POST (GET тоже отвечает 200 и тем же телом).
- Тело: объект фильтра фронта; на ответ **не влияет** — проверял `{}`, `{"regionId":...}`,
  `{"region":...}`, `{"region_id":...}` и выбор региона через cookie (`GET /region/select/2612668`) —
  всегда одни и те же 1925 АЗС.
- Заголовки ответа: `cache-control: no-cache, private`, `x-ratelimit-limit: 1000`,
  `x-ratelimit-remaining: 998`. Лимит 1000 запросов (по всей видимости, в минуту) — для опроса раз
  в 5–15 минут запас огромный.
- Content-Type ответа приходит как `text/html`, но тело — валидный JSON.

Замечание: при заходе через headless-браузер сайт отдал 403 (антибот по UA). Обычный curl с
десктопным User-Agent проходит без проблем. Для бота — ставить нормальный UA.

## Структура ответа

```
{
  "stations":         [...1925],   // все АЗС страны
  "promotions":       [...23],
  "oilProducts":      [...15],     // справочник видов топлива
  "oilProductGroups": [...3],      // группы (ДТ, G-ДТ, ДТ Опти)
  "regions":          [...53]      // id регионов
}
```

Объект АЗС:

```json
{
  "GPNAZSID": 9142,
  "PNPONumber": " 1 ",
  "brand_id": 31,
  "region_id": 2612668,
  "AZSTypeID": 2,
  "id": 885,
  "name": "АЗС №1",
  "city": "Краснодар",
  "address": "Селезнева, 197/2",
  "latitude": "45.01781",
  "longitude": "39.05341",
  "oils": { "12": false, "62": false, "372": true, "421": false, "100032": false },
  "workMode": "круглосуточно",
  "workModeMessages": null,
  "open": true,
  "wash": false,
  "rate": null,
  "rateCount": 0,
  "ChargingElectricVehicles": 0,
  "services": { "station": [], "car": {}, "person": {}, "payment": {} },
  "promotions": [], "promotionsInclude": [], "promotionsExclude": []
}
```

**`oils` — это то, ради чего всё затевалось.** Ключ — id вида топлива, значение — есть/нет прямо сейчас.
Набор ключей = ассортимент конкретной АЗС (то, чем она в принципе торгует).

Подводные камни при парсинге:
- ключи `oils` — **строки**, а id в `oilProducts` — числа;
- если данных по АЗС нет, `oils` приходит **пустым массивом `[]`**, а не объектом (артефакт `json_encode`
  в PHP). Сейчас таких 336 из 1925. В Краснодаре это АЗС №2302 (id 1325);
- `PNPONumber` приходит с пробелами по краям — надо `trim()`.

## Справочник топлива (`oilProducts`)

| id | shortTitle | title |
|---|---|---|
| 1 | ДТа | Дизельное топливо арктическое |
| 12 | 95 | Бензин АИ-95 |
| 21 | 98 | Бензин АИ-98 |
| 62 | 92 | Бензин АИ-92 |
| 372 | ДТл | Дизельное топливо летнее |
| 373 | ГАЗ | СУГ |
| 374 | ДТз | Дизельное топливо зимнее |
| 421 | G-95 | Бензин АИ-95 брендированный |
| 424 | G-ДТ Л | ДТ летнее брендированное |
| 461 | ДТм | Дизельное топливо межсезонное |
| 512 | ДТ | Дизельное топливо |
| 531 | КПГ | КПГ |
| 541 | ДТ Опти | Дизельное топливо Опти |
| 100032 | G-100 | Бензин АИ-100 брендированный |
| 100036 | 100 | Бензин АИ-100 |

## Краснодар

17 АЗС, `city == "Краснодар"` (регион «Краснодарский край» = `region_id 2612668`, всего 66 АЗС по краю).
Актуальный срез на момент ресерча — в [docs/krasnodar-stations.json](docs/krasnodar-stations.json):

| id | АЗС | Адрес | Наличие на 16.08.2026 |
|---|---|---|---|
| 885 | №1 | Селезнева, 197/2 | 95✗ 92✗ G-100✗ ДТл✓ G-95✗ |
| 886 | №2 | Уральская, 194/1 | 95✗ 92✗ ДТл✓ G-95✗ |
| 887 | №3 | 1-я Дорожная, 33 | 95✗ 92✗ ДТл✗ G-95✗ |
| 888 | №4 | Суворова, 2/1 | 95✗ 92✗ ДТл✓ G-95✗ |
| 894 | №10 | Краснодар–Кропоткин–гр. Ставропольского кр. | 95✗ 92✗ G-100✗ ДТл✓ G-95✗ |
| 2705 | №13 | Селезнева, 1 | 95✗ 92✗ G-95✗ |
| 899 | №19 | Ялтинская, 30 | 95✗ 92✗ G-100✗ ДТл✗ G-95✗ |
| 900 | №20 | Уральская, 96/3 | 95✗ 92✗ G-100✗ ДТл✓ G-95✗ |
| 1078 | №46 | Краснодар–Кропоткин–гр. Ставропольского кр. | 95✗ 92✗ G-100✗ ДТл✓ G-95✗ |
| 1237 | №105 | Евдокии Сокол, 10 | 95✓ 92✗ ГАЗ✗ ДТл✗ G-95✗ |
| 1340 | №117 | Кирилла Россинского | 95✗ 92✗ ДТл✗ G-95✗ |
| 1413 | №119 | Западный обход, 59 | 95✗ 92✗ ГАЗ✗ ДТл✗ G-95✗ |
| 1482 | №124 | Калинина, 58, 9 | 95✗ 92✗ ДТл✗ G-95✗ |
| 1560 | №130 | Евдокимовская, 2 | 95✗ 92✗ ГАЗ✗ ДТл✗ G-95✗ |
| 1625 | №135 | Восточно-Кругликовская, 6 | 95✗ 92✗ ДТл✗ G-95✗ |
| 1761 | №136 | Переходная, 3 | 95✗ 92✓ ДТл✓ G-95✗ |
| 1325 | №2302 | Красных Партизан, 67 | нет данных (`oils: []`) |

Картина совпадает с реальным дефицитом бензина в крае: живой 92/95 в моменте есть на 2 АЗС из 17,
дизель — на 6. То есть бот действительно имеет смысл.

## Ссылка на конкретную АЗС на карте

```
https://gpnbonus.ru/fuel/refuel-map#station=885&CenterLat=45.01781&CenterLon=39.05341
```

Фронт разбирает эти параметры из hash (`parseInputParams`) и открывает карточку АЗС.

## Что это значит для бота

1. **Поллинг** `POST /api/stations/list` раз в 5–10 минут, `--compressed`, десктопный UA.
   1000 запросов лимита — потолок недостижим.
2. Фильтруем локально: `city == "Краснодар"` либо по явному списку `id` (пользователь выбирает свои АЗС).
   Список id стабилен — можно захардкодить/хранить в БД подписок.
3. Храним предыдущий снимок `oils` по каждой отслеживаемой АЗС и **шлём уведомление на переход
   `false → true`** по нужному виду топлива (и опционально `true → false`).
4. Нюансы, которые надо заложить: `oils` бывает `[]`; ключи-строки; `PNPONumber` с пробелами;
   антибот по UA; ответ приходит с `content-type: text/html`.
5. Дребезг: флаги дёргаются часто (10 изменений по стране за минуту наблюдения) — стоит либо
   подтверждать переход двумя подряд опросами, либо ставить cooldown на повторное уведомление
   по паре (АЗС, топливо).

## Открытые вопросы

- Мобильное приложение ходит на `api.gpnbonus.ru` (тот же IP 213.221.41.242). Не копал — возможно,
  там есть более лёгкий endpoint с фильтром по региону. Пока не нужно: 165 КБ раз в 5 минут — ничто.
- ToS сайта на предмет автоматизированного доступа не смотрел. Нагрузка минимальная, но помнить стоит.
